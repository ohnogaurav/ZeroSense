name: zero
description: A new Flutter project.

environment:
  sdk: "^3.10.0"

dependencies:
  flutter:
    sdk: flutter


  firebase_core: ^3.4.0
  firebase_auth: ^5.3.2
  firebase_database: ^11.1.2

  provider: ^6.1.2
  http: ^1.2.1
  # add under dependencies
  shared_preferences: ^2.1.1
  audioplayers: ^6.5.1
  lottie: ^2.4.0
  flutter_launcher_icons: ^0.14.4 # dev tool
  flutter_native_splash: ^2.3.8  # dev tool
  uuid: ^3.0.7
  flutter_lints: ^6.0.0
  rename: ^3.1.0


flutter:
  uses-material-design: true

dev_dependencies:
  flutter_launcher_icons: ^0.14.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
